/* -*- C++ -*- */




input string Just_Label = "Configs do Just";  // -------------------XANDROS-----------------
bool just_tempo_real = false; //Tempo Real (onTick - true) vs Candle (false)
input int just_distancia_compra = 70; //Distancia para compra (TickSize)
input int just_distancia_venda = 70; //Distancia para venda (TickSize)

input bool just_compra = true;
input bool just_venda = true;
